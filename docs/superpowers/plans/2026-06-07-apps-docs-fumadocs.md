# apps/docs Fumadocs Site Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stand up a Fumadocs (Next.js App Router) documentation site at `apps/docs` for the agentkit SDK, seeded language-agnostically from `docs/AGENTKIT.md`, wired into the Bun + Turbo monorepo as `@agentkit/docs`.

**Architecture:** Scaffold with the official `create-fumadocs-app` (the source of truth for the current known-good layout), then adapt package metadata, scripts, gitignore, and Turbo wiring to match sibling workspaces (`@agentpay/web`, `@agentkit/sdk`). Replace the scaffold's sample content with ~9 MDX pages mapped from `docs/AGENTKIT.md`. Verify by build + `tsc` + gstack `/browse` dogfood.

**Tech Stack:** Next.js (App Router), `fumadocs-core` / `fumadocs-ui` / `fumadocs-mdx`, MDX, TypeScript, Bun 1.3, Turbo 2.x, oxfmt.

## Testing note (read first)

This deliverable is a scaffolded docs site. There is **no application logic to unit-test** — no functions, no business rules. Following the spec's verification section, the "tests" in this plan are **build / typecheck / render gates**, not failing-unit-tests-first:

- `next build` passing = the site compiles and all MDX resolves.
- `tsc --noEmit` passing = types are sound.
- gstack `/browse` dogfood = pages actually render in a browser.

Do not invent hollow unit tests (e.g. "assert index.mdx exists"). The build gate already covers content wiring. TDD discipline here means: make each change small, run the relevant gate immediately after, and commit on green.

## Source-of-truth caveat (read second)

`fumadocs.dev` is unreachable on this network (corporate proxy blocks it) and Context7 was down at planning time, so the **exact** post-scaffold file layout (whether content lives in `content/docs/` vs `app/docs/`, the precise shape of `source.config.ts` / `lib/source.ts`) could not be pre-confirmed against current docs. This is deliberate, not a gap:

- **Task 1 scaffolds first, then Task 2 reads the actual generated tree** and records the real paths before any content work. Every later task references "the content root" and "the source config" by role; the executor substitutes the real paths discovered in Task 2.
- If a step's assumed path differs from what the scaffold produced, trust the scaffold output, not this document's example path.

## File Structure

Created by the scaffolder (Task 1), exact names confirmed in Task 2. Expected shape (Fumadocs Next.js App Router app):

```
apps/docs/
  package.json              -> adapted in Task 3 (@agentkit/docs, scripts)
  .gitignore                -> written in Task 3 (.next, .source)
  next.config.* (mjs/ts)    -> from scaffold, wraps fumadocs-mdx
  source.config.ts          -> from scaffold, MDX collections config
  tsconfig.json             -> from scaffold
  app/
    layout.tsx              -> root layout
    (home)/ or page.tsx     -> landing (scaffold default; left or trimmed)
    docs/
      layout.tsx            -> DocsLayout, pulls page tree from source
      [[...slug]]/page.tsx  -> renders an MDX page by slug
    api/search/route.ts     -> built-in Orama search route (scaffold default)
  lib/
    source.ts               -> loader() binding content to the route tree
  content/docs/             -> MDX content root (EXACT NAME PER SCAFFOLD)
    index.mdx               -> Task 4
    getting-started.mdx     -> Task 5
    architecture.mdx        -> Task 6
    reference-service.mdx   -> Task 7
    concepts/
      events.mdx            -> Task 8
      session-state.mdx     -> Task 8
      tool-registry.mdx     -> Task 8
      event-bus.mdx         -> Task 8
      providers.mdx         -> Task 8
    meta.json (+ concepts/meta.json) -> Task 9 (nav order)
```

Modified:
- `turbo.json` (root) -> Task 3 (build outputs / per-package override)
- root `bun.lock` -> regenerated by `bun install` in Task 3

Untouched (per spec): all existing `docs/*.md`, including `docs/AGENTKIT.md`.

---

### Task 1: Scaffold the Fumadocs app

**Files:**
- Create: `apps/docs/**` (generated by scaffolder)

- [ ] **Step 1: Confirm on the feature branch**

Run: `cd /Users/Enkang.Yuan1/Desktop/Projects/alloy && git branch --show-current`
Expected: `feat/apps-docs-fumadocs`
If not, run: `git checkout feat/apps-docs-fumadocs`

- [ ] **Step 2: Run the scaffolder into apps/docs**

The scaffolder is interactive. Run it and answer prompts as follows:
- project name / directory: `apps/docs` (or run from inside `apps/` with name `docs`)
- package manager: **npm** if asked (do NOT let it run install with a non-bun PM that writes a competing lockfile; if it offers "skip install", choose skip — we install with bun at root in Task 3)
- template: the default **Next.js** template
- content source: default (`fumadocs-mdx`)
- TypeScript: **yes**
- Tailwind: accept the scaffold default (Fumadocs UI uses it)

Run from repo root:
```bash
cd /Users/Enkang.Yuan1/Desktop/Projects/alloy/apps
bunx create-fumadocs-app@latest docs
```

If it insists on installing, let it finish, then delete any nested lockfile it created that is not bun:
```bash
rm -f /Users/Enkang.Yuan1/Desktop/Projects/alloy/apps/docs/package-lock.json \
      /Users/Enkang.Yuan1/Desktop/Projects/alloy/apps/docs/pnpm-lock.yaml \
      /Users/Enkang.Yuan1/Desktop/Projects/alloy/apps/docs/yarn.lock
```
Also delete a nested `node_modules` if created (root bun install repopulates):
```bash
rm -rf /Users/Enkang.Yuan1/Desktop/Projects/alloy/apps/docs/node_modules
```

Expected: `apps/docs/` exists with `package.json`, `app/`, a content directory, `source.config.*`, `next.config.*`, `tsconfig.json`.

- [ ] **Step 3: Do NOT commit yet**

Leave the scaffold uncommitted; Task 2 inspects it and Task 3 adapts it. Committing happens at the end of Task 3 once it is repo-shaped.

---

### Task 2: Map the actual scaffold output

No file changes. This task records reality so later tasks use correct paths.

- [ ] **Step 1: Print the generated tree**

Run:
```bash
cd /Users/Enkang.Yuan1/Desktop/Projects/alloy/apps/docs && \
find . -type f -not -path './node_modules/*' -not -path './.next/*' -not -path './.source/*' | sort
```

- [ ] **Step 2: Read the key files and record their real paths**

Read each of these (whichever exist) and note exact locations:
```bash
cat apps/docs/package.json
cat apps/docs/source.config.* 2>/dev/null
cat apps/docs/lib/source.ts 2>/dev/null || cat apps/docs/app/source.ts 2>/dev/null
cat apps/docs/next.config.* 2>/dev/null
ls apps/docs/content/docs 2>/dev/null || ls apps/docs/app/docs 2>/dev/null
cat apps/docs/app/docs/**/page.tsx 2>/dev/null | head -60
```

- [ ] **Step 3: Write down the substitutions**

Record, for use in Tasks 4-9:
- CONTENT_ROOT = actual content dir (e.g. `content/docs/`)
- META_FORMAT = how nav order is declared (`meta.json` keys, or frontmatter)
- The frontmatter fields the scaffold's pages use (usually `title`, `description`)

No commit (read-only task).

---

### Task 3: Adapt to monorepo conventions, then install + commit

**Files:**
- Modify: `apps/docs/package.json`
- Create: `apps/docs/.gitignore`
- Modify: `turbo.json` (root)

- [ ] **Step 1: Rewrite apps/docs/package.json fields**

Set name/version/private and align scripts. Keep the scaffold's dependencies and Next-specific config; only change the fields below. Edit `name`, ensure `private`, set `version`, and replace the `scripts` block:

```jsonc
{
  "name": "@agentkit/docs",
  "version": "0.0.0",
  "private": true,
  // ...keep "type", scaffold deps/devDeps as generated...
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "format": "oxfmt --write \"**/*.{ts,tsx}\"",
    "format:check": "oxfmt --check \"**/*.{ts,tsx}\"",
    "clean": "rm -rf .next .source node_modules/.tmp out"
  }
}
```

Notes:
- Scope `format` to `.ts,.tsx` only (spec open-risk: oxfmt may fight MDX/Next idioms; leave MDX to Fumadocs defaults).
- If the scaffold has no `next lint` setup, set `"lint": "echo \"(no lint configured)\" && exit 0"` so `turbo run lint` does not error. Prefer real `next lint` if the scaffold provides eslint config.
- If the scaffold already defines `postinstall` (fumadocs-mdx runs `fumadocs-mdx` to generate `.source`), KEEP it.

- [ ] **Step 2: Write apps/docs/.gitignore**

Create `apps/docs/.gitignore`:
```gitignore
# dependencies
node_modules

# next.js
.next
out

# fumadocs generated
.source

# production
build
dist

# misc
.DS_Store
*.local
*.log
npm-debug.log*

# typescript
*.tsbuildinfo

# env
.env
.env*.local
!.env.example
```

- [ ] **Step 3: Make Turbo aware of Next build output**

Read root `turbo.json`. The shared `build` task currently has `outputs: ["dist/**", "bin/**", "src-tauri/target/**"]` — Next emits `.next/**`. Add a per-package override mirroring the existing `@agentpay/*#build` style so the docs build caches correctly without changing other packages.

Modify `turbo.json` `tasks` object, add:
```jsonc
"@agentkit/docs#build": {
  "dependsOn": ["^build"],
  "outputs": [".next/**", "!.next/cache/**"]
},
"@agentkit/docs#dev": {
  "cache": false,
  "persistent": true
}
```

- [ ] **Step 4: Install from repo root with bun**

Run:
```bash
cd /Users/Enkang.Yuan1/Desktop/Projects/alloy && bun install
```
Expected: completes; output shows `@agentkit/docs` among workspace packages. `apps/docs/node_modules` is now symlinked/populated via the root workspace.

- [ ] **Step 5: Verify the workspace is recognized**

Run:
```bash
cd /Users/Enkang.Yuan1/Desktop/Projects/alloy && bun pm ls 2>/dev/null | grep -i "agentkit/docs" || cat package.json | grep -A4 workspaces
```
Expected: `@agentkit/docs` resolvable (workspace glob `apps/*` already covers it).

- [ ] **Step 6: Run the production build (first gate)**

Run:
```bash
bun --filter @agentkit/docs build
```
Expected: `next build` succeeds (it will build the scaffold's sample content; that's fine — content is replaced in Tasks 4-9). If it fails on a missing `.source`, run the generate step the scaffold defines (often `bunx fumadocs-mdx` or it runs via `postinstall`/prebuild) and rebuild.

- [ ] **Step 7: Typecheck gate**

Run:
```bash
bun --filter @agentkit/docs typecheck
```
Expected: no type errors.

- [ ] **Step 8: Format the TS/TSX**

Run:
```bash
bun --filter @agentkit/docs format
```
Expected: oxfmt rewrites/inspects only `.ts`/`.tsx`. If oxfmt errors on scaffold syntax it cannot parse, narrow the glob to the files it can handle or skip formatting the offending file; do not let formatting block the task.

- [ ] **Step 9: Commit the adapted scaffold**

```bash
cd /Users/Enkang.Yuan1/Desktop/Projects/alloy
git add apps/docs turbo.json bun.lock
git commit -m "feat(docs): scaffold @agentkit/docs fumadocs app

create-fumadocs-app scaffold adapted to monorepo conventions:
@agentkit/docs name, aligned scripts, gitignore, Turbo build override.
Content is still the scaffold sample; replaced in following commits.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: Write index.mdx (overview)

**Files:**
- Create/replace: `<CONTENT_ROOT>/index.mdx` (from Task 2; likely `content/docs/index.mdx`)

Source mapping: `docs/AGENTKIT.md` lines 1-23 (intro + packages table) + the language-neutral framing from the spec. Use the scaffold's frontmatter fields (from Task 2).

- [ ] **Step 1: Replace the scaffold index with agentkit overview**

Write `<CONTENT_ROOT>/index.mdx` (adjust frontmatter keys to match scaffold convention recorded in Task 2):

```mdx
---
title: agentkit
description: Embeddable SDK for building agents — event-sourced runtime, tool registry, pluggable LLM providers, and STT/TTS modalities.
---

agentkit is an embeddable SDK for building agents: an event-sourced runtime, a
tool registry, pluggable LLM providers, and STT/TTS modalities. the core is
infra-free. no database or server is required to import and use it. deploy it
yourself, or run the reference service (`agentkit-serve`) when you need a
production-grade multi-process setup.

it powers agentpay's agent runtime, and can also be used standalone in any
Python or TypeScript project.

## packages

| package | path | what it is |
| ---------------- | ---------------- | --------------------------------------------- |
| `agentkit` | `agentkit/sdk` | Python SDK: the core runtime, embed anywhere |
| `agentkit-serve` | `agentkit/serve` | Python: FastAPI + workers reference service |
| `@agentkit/sdk` | `agentkit/ts` | TypeScript port of the core runtime |

The concepts in these docs are stated language-neutrally; code samples show
Python and TypeScript where both exist. The wire format (event type strings,
field names) is identical across the Python SDK and the TypeScript port, so
events round-trip across both.

## when to embed vs. run the service

Embed `agentkit` directly for infra-free usage inside your own app. Run
`agentkit-serve` when you need multi-process durability and real-time voice.
See [the reference service](/docs/reference-service).
```

- [ ] **Step 2: Build gate**

Run: `bun --filter @agentkit/docs build`
Expected: succeeds; `/` (index) compiles. (Internal link `/docs/reference-service` resolves once Task 7 lands; Fumadocs does not hard-fail builds on a not-yet-existing internal link, but if it does, leave the link as plain text until Task 7, then restore.)

- [ ] **Step 3: Commit**

```bash
git add apps/docs && git commit -m "docs(content): index / overview page

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: Write getting-started.mdx

**Files:**
- Create: `<CONTENT_ROOT>/getting-started.mdx`

Source mapping: install lines from root `README.md` (bun install; poetry install) + the tool-registry code samples from `docs/AGENTKIT.md` lines 88-103, reframed as a first agent. Keep code neutral; show both languages.

- [ ] **Step 1: Write the page**

```mdx
---
title: Getting started
description: Install agentkit and register your first tool.
---

## install

```bash
# TypeScript (from the monorepo JS workspace)
bun install
# the package is @agentkit/sdk

# Python
cd agentkit/sdk && poetry install
```

## register a tool

Tools are a spec (name, description, JSON-schema parameters) plus a handler.
The runtime sends the full tool-spec list to the model; when the model requests
a call, the registry dispatches the handler. Multiple calls from one model turn
run concurrently and results are collected before the next loop iteration.

```python
# Python
@agentkit.register_tool(
    agentkit.tool_spec_from_model("get_weather", "Look up weather", GetWeather)
)
async def get_weather(ctx: agentkit.ToolContext, args: dict) -> dict:
    return {"tempF": 68}
```

```ts
// TypeScript
registerTool(
  toolSpecFromSchema("get_weather", "Look up weather", z.object({ city: z.string() })),
  async (ctx, args) => ({ tempF: 68 }),
);
```

`ToolContext` carries `userId` / `user_id` and an optional `db` handle (null by
default; inject one when the tool needs persistence).

## next

Read [core concepts](/docs/concepts/events) to understand events, session
state, the tool registry, the event bus, and providers.
```

- [ ] **Step 2: Build gate**

Run: `bun --filter @agentkit/docs build`
Expected: succeeds.

- [ ] **Step 3: Commit**

```bash
git add apps/docs && git commit -m "docs(content): getting-started page

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 6: Write architecture.mdx

**Files:**
- Create: `<CONTENT_ROOT>/architecture.mdx`

Source mapping: `docs/AGENTKIT.md` lines 25-58 (both ASCII diagrams + surrounding prose). Keep the diagrams in fenced code blocks so they render verbatim.

- [ ] **Step 1: Write the page**

```mdx
---
title: Architecture
description: The modality-agnostic ReAct runtime loop and where voice attaches.
---

## the runtime loop

```
   ┌────────────────────────────────────────────────────┐
   │  your app  (or agentpay/api)                       │
   │  session manager   tool registry   model provider  │
   └────────────────────────┬───────────────────────────┘
                            ▼
   ┌───────────────────────────────────────────────────┐
   │  agent runtime  (ReAct loop)                      │
   │  1. project session state from event log          │
   │  2. call LLM provider with history + tool specs   │
   │  3. execute tool calls scatter-gather             │
   │  4. loop until model response is final            │
   └────────┬────────────────────────────┬─────────────┘
            │ events                     │ tool calls
            ▼                            ▼
   ┌──────────────────┐        ┌──────────────────────┐
   │  event store     │        │  tool handlers       │
   │  (in-memory or   │        │  (your functions,    │
   │   persistent)    │        │   run concurrently)  │
   └──────────────────┘        └──────────────────────┘
```

## modalities attach at the edges

The reasoning loop is modality-agnostic. Voice adds STT at the input edge and
TTS at the output edge; text skips both.

```
   voice:   audio → STT → [runtime loop] → TTS → audio
   text:    text  →       [runtime loop] →       text
```
```

- [ ] **Step 2: Build gate**

Run: `bun --filter @agentkit/docs build`
Expected: succeeds; fenced diagrams render as code blocks.

- [ ] **Step 3: Commit**

```bash
git add apps/docs && git commit -m "docs(content): architecture page

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 7: Write reference-service.mdx

**Files:**
- Create: `<CONTENT_ROOT>/reference-service.mdx`

Source mapping: `docs/AGENTKIT.md` lines 126-141 (agentkit-serve, the three-process table).

- [ ] **Step 1: Write the page**

```mdx
---
title: Reference service
description: agentkit-serve wraps the SDK as three processes over Redis for multi-process durability and real-time voice.
---

`agentkit-serve` (`agentkit/serve`) wraps the SDK as three processes over Redis
so heavy tool execution never stalls a real-time exchange.

| process | role |
| ------------ | --------------------------------------------------------- |
| `api` | FastAPI app: REST routes and STT WebSocket |
| `bus-worker` | reasoning loop: LLM calls, event bus, tool dispatch |
| `worker` | async tool execution (TaskIQ), results back to bus-worker |

Redis Streams provide durable at-least-once hand-off between processes. Redis
Pub/Sub fans out agent responses to the connected client in real time.

Use `agentkit-serve` when you need multi-process durability and real-time
voice. Embed `agentkit` directly when you want infra-free usage inside your own
app.
```

- [ ] **Step 2: Build gate + restore any deferred link**

Run: `bun --filter @agentkit/docs build`
Expected: succeeds. If Task 4 step 2 had to downgrade the `/docs/reference-service` link to plain text, restore it to a link now and rebuild.

- [ ] **Step 3: Commit**

```bash
git add apps/docs && git commit -m "docs(content): reference-service page

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 8: Write the five concept pages

**Files:**
- Create: `<CONTENT_ROOT>/concepts/events.mdx`
- Create: `<CONTENT_ROOT>/concepts/session-state.mdx`
- Create: `<CONTENT_ROOT>/concepts/tool-registry.mdx`
- Create: `<CONTENT_ROOT>/concepts/event-bus.mdx`
- Create: `<CONTENT_ROOT>/concepts/providers.mdx`

Source mapping: `docs/AGENTKIT.md` lines 60-124 (the "core concepts" subsections), one page each. Reframe per the spec's correctness note: state concepts neutrally; do NOT copy the "not yet ported" sentence.

- [ ] **Step 1: events.mdx**

```mdx
---
title: Events
description: All session state derives from an append-only event log.
---

All session state is derived from an append-only event log. Events are
discriminated by `type` (e.g. `user.message`, `tool.call.completed`,
`agent.message.completed`). The full list lives in the SDK source:

- `agentkit/sdk/agentkit/infra/events/schemas.py` (Python)
- `agentkit/ts/src/events/schemas.ts` (TypeScript)

The event-type string values are the wire format and are identical across both
SDKs, so an event log written by one can be replayed by the other.
```

- [ ] **Step 2: session-state.mdx**

```mdx
---
title: Session state
description: replaySession projects an event log into the conversation passed to the model.
---

`replaySession` (Python: `ReplaySession`) takes the event log for a session and
projects it into a `SessionState`: `isActive`, and `messages` — the conversation
history in `{ role, content }` form that gets passed to the LLM.

Because state is a pure projection of the log, replay is deterministic: the same
events always yield the same `SessionState`.
```

- [ ] **Step 3: tool-registry.mdx**

```mdx
---
title: Tool registry
description: Register a tool spec plus a handler; calls from one model turn run concurrently.
---

Tools are registered with a spec (name, description, JSON-schema parameters) and
a handler function. The runtime calls the LLM with the full tool-spec list; when
the model requests a tool call, the registry dispatches it. Multiple tool calls
from one LLM turn run concurrently (scatter-gather) and results are collected
before the next loop iteration.

```python
# Python
@agentkit.register_tool(
    agentkit.tool_spec_from_model("get_weather", "Look up weather", GetWeather)
)
async def get_weather(ctx: agentkit.ToolContext, args: dict) -> dict:
    return {"tempF": 68}
```

```ts
// TypeScript
registerTool(
  toolSpecFromSchema("get_weather", "Look up weather", z.object({ city: z.string() })),
  async (ctx, args) => ({ tempF: 68 }),
);
```

`ToolContext` carries `userId` / `user_id` and an optional `db` handle (null by
default; inject one when the tool needs persistence).
```

- [ ] **Step 4: event-bus.mdx**

```mdx
---
title: Event bus
description: The bus fans out events to per-session subscribers.
---

The event bus fans out events to subscribers per session. In the Python SDK the
default implementation is in-memory (`InMemoryEventBus`); a Redis Stream-backed
bus is used in `agentkit-serve` for cross-process durability. The TypeScript SDK
ships an in-memory bus, which is sufficient for an embedded SDK.
```

- [ ] **Step 5: providers.mdx**

```mdx
---
title: Providers
description: LLM, TTS, and STT providers implement common interfaces selected by configuration.
---

LLM providers implement a common interface. The Python SDK ships `kimi`
(OpenRouter/Kimi, the default), `openai`, and `mock` (for tests), selected via
`AGENTKIT_MODEL_PROVIDER`. Adding a provider means implementing the
`ModelProvider` protocol and registering it.

TTS providers (`gemini`, `openai`, `none`) follow the same pattern via the
`TTSProvider` protocol. STT uses Soniox by default.

The TypeScript port mirrors the same neutral message and tool-spec format, so a
provider translates to its own API only at its own boundary; the runtime never
imports provider-specific types.
```

- [ ] **Step 6: Build gate**

Run: `bun --filter @agentkit/docs build`
Expected: succeeds; all five concept pages compile.

- [ ] **Step 7: Commit**

```bash
git add apps/docs && git commit -m "docs(content): five concept pages

events, session-state, tool-registry, event-bus, providers.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 9: Navigation order

**Files:**
- Create/modify: `<CONTENT_ROOT>/meta.json`
- Create: `<CONTENT_ROOT>/concepts/meta.json`

Use the META_FORMAT recorded in Task 2. Fumadocs `meta.json` typically takes `{ "title": ..., "pages": [...] }` where `pages` lists file/folder slugs in order, and `"..."` means "everything else."

- [ ] **Step 1: Top-level meta.json**

Write `<CONTENT_ROOT>/meta.json`:
```json
{
  "title": "agentkit",
  "pages": [
    "index",
    "getting-started",
    "concepts",
    "architecture",
    "reference-service"
  ]
}
```

- [ ] **Step 2: concepts/meta.json**

Write `<CONTENT_ROOT>/concepts/meta.json`:
```json
{
  "title": "Concepts",
  "pages": [
    "events",
    "session-state",
    "tool-registry",
    "event-bus",
    "providers"
  ]
}
```

- [ ] **Step 3: Build gate**

Run: `bun --filter @agentkit/docs build`
Expected: succeeds; nav order reflects the meta files.

- [ ] **Step 4: Commit**

```bash
git add apps/docs && git commit -m "docs(content): sidebar navigation order

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 10: Full verification (build + typecheck + lint + browse dogfood)

No new files. This is the spec's verification section, executed.

- [ ] **Step 1: Clean build from root via Turbo**

Run:
```bash
cd /Users/Enkang.Yuan1/Desktop/Projects/alloy
bun --filter @agentkit/docs clean
turbo run build --filter=@agentkit/docs
```
Expected: build succeeds through Turbo (confirms the `@agentkit/docs#build` override + outputs are correct).

- [ ] **Step 2: Typecheck + lint do not regress**

Run:
```bash
turbo run typecheck --filter=@agentkit/docs
turbo run lint --filter=@agentkit/docs
```
Expected: both succeed (lint may be a no-op echo if the scaffold had no eslint; that is acceptable per Task 3).

- [ ] **Step 3: Start the dev server (background)**

Run (background process):
```bash
bun --filter @agentkit/docs dev
```
Note the port from output (Next default `3000`; if taken it picks another — record the actual URL).

- [ ] **Step 4: Dogfood with the gstack /browse skill**

Per `CLAUDE.md`, use `/browse` for all web browsing (never the chrome MCP tools directly). Invoke the `browse` skill against the running dev URL and:
- load `/` (or `/docs`) and confirm the landing renders
- open `/docs` (overview), confirm the sidebar shows: agentkit, getting-started, Concepts (group), architecture, reference-service in that order
- open one concept page (`/docs/concepts/events`) and confirm prose + that code blocks render
- open `/docs/architecture` and confirm the ASCII diagrams render inside code blocks
- check the browser console for errors via the browse skill's console read

Record findings. Fix any render issue (e.g. broken internal link, missing frontmatter title) and re-run the relevant build gate + reload.

- [ ] **Step 5: Stop the dev server**

Stop the background dev process.

- [ ] **Step 6: Final commit if fixes were made**

If Step 4 required fixes:
```bash
git add apps/docs && git commit -m "fix(docs): resolve render issues found in browse dogfood

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```
If no fixes were needed, no commit.

- [ ] **Step 7: Report**

Summarize: build/typecheck/lint results (with the actual command output), what the browse dogfood confirmed, the dev URL, and the list of pages. Do NOT run `/ship` or open a PR unless the user asks.

---

## Self-review

**Spec coverage** (checked each spec section against a task):

- Scope = agentkit SDK only, language-agnostic -> Tasks 4-9 content; no agentpay page. ✓
- Scaffold via create-fumadocs-app, adapt to conventions -> Tasks 1, 3. ✓
- package name `@agentkit/docs`, private, version 0.0.0, aligned scripts -> Task 3 step 1. ✓
- gitignore `.next`/`.source` -> Task 3 step 2. ✓
- Turbo build outputs / override -> Task 3 step 3. ✓
- One root `bun install` -> Task 3 step 4. ✓
- Content structure (index, getting-started, 5 concepts, architecture, reference-service, meta) -> Tasks 4-9, matches spec's tree. ✓
- Content mapping from AGENTKIT.md sections -> per-task "Source mapping" lines. ✓
- Correctness note: no "not yet ported" verbatim, language-neutral -> Task 8 instruction + neutral phrasing throughout. ✓
- Prose style: terse, no em-dashes -> all content drafted that way. ✓
- Branch `feat/apps-docs-fumadocs`, not main -> Task 1 step 1. ✓
- Verification: install, dev, build, typecheck/lint, /browse dogfood -> Task 10. ✓
- Do NOT touch existing docs/*.md -> no task modifies them; AGENTKIT.md only read as source. ✓
- YAGNI: no extra search backend (use scaffold default), no deploy config -> nothing adds them. ✓
- oxfmt MDX risk -> Task 3 step 1 scopes format to .ts/.tsx. ✓
- Leverage gstack tools -> Task 10 step 4 uses /browse per CLAUDE.md. ✓

**Placeholder scan:** No "TBD"/"add error handling"/"similar to Task N". The CONTENT_ROOT / META_FORMAT substitutions are explicit, justified by the unreachable-docs caveat, and resolved concretely in Task 2 before first use. ✓

**Type/name consistency:** Script names (`dev`/`build`/`typecheck`/`format`/`format:check`/`clean`) match across Task 3 and Task 10 commands. Package name `@agentkit/docs` consistent. Turbo override keys (`@agentkit/docs#build`, `@agentkit/docs#dev`) consistent. Content filenames in Tasks 4-9 match the meta.json `pages` entries in Task 9 (`index`, `getting-started`, `concepts`, `architecture`, `reference-service`; concepts: `events`, `session-state`, `tool-registry`, `event-bus`, `providers`). ✓

No gaps found.
