id: kaji-python-public-factory-no-origin-override
valid:
  - |
      def for_gmail(token_for: TokenProvider):
          return build(token_for)
  - |
      def for_github(token_for: TokenProvider):
          return build(token_for)
  - |
      def _create_google_oauth_client_for_test(client: Client):
          return build(client)
  - |
      def _for_gmail_for_test(origin: str, transport: Transport):
          return build(origin, transport)
invalid:
  - |
      def for_gmail(origin: str):
          return build(origin)
  - |
      def for_github(base_url: str):
          return build(base_url)
  - |
      def create_google_oauth_client(client: Client):
          return build(client)
