# ── Secrets & environment ────────────────────────────────────────────────────
.env
.env.*
!.env.example
!.env.*.example
.env.keys

# ── OS ───────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Editors & IDEs ───────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── AI / LLM tool state ──────────────────────────────────────────────────────
.claude/
.cursor/
.gemini/
.kiro/
.gstack/
.antigravitycli/
.superpowers/
tools/
!tools/
tools/*
!tools/ast-grep/
tools/ast-grep/*
!tools/ast-grep/rules/
!tools/ast-grep/rules/**
!tools/ast-grep/rule-tests/
!tools/ast-grep/rule-tests/**

# ── Node / Bun ───────────────────────────────────────────────────────────────
node_modules/
bun.lock
!bun.lock

# ── Build output ─────────────────────────────────────────────────────────────
dist/
out/
build/
.next/
*.tsbuildinfo

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.eggs/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.mypy_cache/
.ruff_cache/
.pyrefly/
.tox/
.nox/
.hypothesis/
MANIFEST

# ── Virtual environments ─────────────────────────────────────────────────────
.venv
venv/
env/
ENV/

# ── Logs ─────────────────────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*

# ── Local databases ───────────────────────────────────────────────────────────
*.db
*.sqlite
*.sqlite3

# ── Turborepo ────────────────────────────────────────────────────────────────
.turbo/

# ── Docker / Supabase volumes ────────────────────────────────────────────────
docker/supabase/volumes/db/data/
docker/supabase/volumes/storage/

# ── Go binaries ──────────────────────────────────────────────────────────────
ryo/api/bin/
ryo/consumer/consumer

# ── iOS / Xcode ──────────────────────────────────────────────────────────────
apps/havenos/Config.xcconfig
**/Config.xcconfig
!**/Config.xcconfig.example
*.xcuserstate
xcuserdata/
DerivedData/
*.ipa
*.dSYM
.build/

# ── Misc ─────────────────────────────────────────────────────────────────────
*.tmp
*.bak
imgs/

# ── Scoped re-includes ───────────────────────────────────────────────────────
# Python's `lib/` rule above would also swallow the docs app's TS source tree.
# There is no `lib/` rule above — removed. Previously needed negation is gone.
