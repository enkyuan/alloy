# Kaji CLI

This legacy path is retained for existing links. Use the maintained
[CLI reference](../../apps/docs/content/cli.mdx), which distinguishes the
Python SDK, embedded `@kaji/sdk`, and standalone `@kaji/cli` command surfaces.
