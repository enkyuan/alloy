name: format / go

on:
  push:
    branches: [main]
    paths:
      - "ryo/api/**"
      - "ryo/consumer/**"
      - ".github/actions/setup-go-cache/**"
      - ".github/workflows/go.format.yml"
  pull_request:
    paths:
      - "ryo/api/**"
      - "ryo/consumer/**"
      - ".github/actions/setup-go-cache/**"
      - ".github/workflows/go.format.yml"

jobs:
  api:
    name: ryo/api
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ryo/api
    steps:
      - uses: actions/checkout@v4

      - uses: ./.github/actions/setup-go-cache
        with:
          working-directory: ryo/api

      - name: Check formatting
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "Unformatted files:"
            echo "$unformatted"
            exit 1
          fi

  consumer:
    name: ryo/consumer
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ryo/consumer
    steps:
      - uses: actions/checkout@v4

      - uses: ./.github/actions/setup-go-cache
        with:
          working-directory: ryo/consumer

      - name: Check formatting
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "Unformatted files:"
            echo "$unformatted"
            exit 1
          fi
