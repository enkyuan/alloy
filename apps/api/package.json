{
  "name": "@agentpay/api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "go run ./cmd/api/main.go",
    "build": "go build -o bin/api ./cmd/api/main.go",
    "lint": "echo 'go lint runs in CI'",
    "clean": "rm -rf bin"
  }
}
