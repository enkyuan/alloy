{
  "name": "agentkit",
  "private": true,
  "workspaces": [
    "packages/*",
    "demos/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "format:check": "turbo run format:check",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean",
    "prepare": "husky",
    "docker:up": "docker compose -f apps/docker/docker-compose.yml up -d",
    "docker:down": "docker compose -f apps/docker/docker-compose.yml down",
    "docker:destroy": "docker compose -f apps/docker/docker-compose.yml down -v --remove-orphans",
    "docker:logs": "docker compose -f apps/docker/docker-compose.yml logs -f"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "husky": "^9.1.0",
    "oxlint": "^1.1.0",
    "oxfmt": "^0.41.0"
  },
  "packageManager": "bun@1.3.11",
  "engines": {
    "node": ">=22.0.0"
  }
}